git checkout [file-name]
git restore --staged [file name]
git reset HEAD ~ [number]
git branch testing   -> переключения -> $ git checkout testing (что бы при создании новой ветки и сразу на нее переключиться – git checkout -b
Отличие между – fast-forward merge & non fast-forward merge- в том, что они могут отфильтровать этот список для вывода только тех веток , которые слиты или еще не слиты в текущую ветку. 
Если произошло изменение в одного файла в одной  той же части файла по- разному в двух объединяемых ветках, git – не может их чисто объединить. Git не создал коммит слияния автоматически .  Он останавливает процесс до тех пор, пока не разрешится конфликт. Что бы посмотреть какие файлы не объединены, можно запустить <git status>. Что бы разрешить конфликт, придется выбрать один из вариантов , либо объединить содержимое. Разрешив каждый конфликт во всех файлах, запускаем < git add > для каждого файла . Запускаем   <git status> , чтоб убедиться в отсутствии конфликтов. 
Да, можно удалить ветку <git branch -d (удалить -какую ветку) test 1 >.
